## Agent skills

### Issue tracker

GitHub issues (via `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

The default 5 canonical roles. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (one `CONTEXT.md`). See `docs/agents/domain.md`.
